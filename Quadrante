const fs = require("fs");
const input = fs.readFileSync(0, "utf8");

let X = parseInt(input.trim());
let divisores = [];

for (let i = 1; i * i <= X; i++) {
    if (X % i === 0) {
        divisores.push(i);
        if (i !== X / i) {
            divisores.push(X / i);
        }
    }
}

divisores.sort((a, b) => a - b); // ordena em ordem crescente

console.log(divisores.join(" "));
